version: '3.8'

services:
  aegis-voice-api:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    container_name: aegis_voice_detection_service
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
